Alembic environment for the Weather & Wellness backend.

Migrations are sourced from `backend/alembic/versions/`.

Alembic reads `DATABASE_MIGRATION_URL` first, then falls back to
`DATABASE_URL`. Set `DATABASE_MIGRATION_URL` to the Supabase session pooler or
direct database URL; avoid the transaction pooler for migrations.
